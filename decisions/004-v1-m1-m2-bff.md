# v1 · M1 迁移就绪 + M2 BFF 落地（2026-08-29）

- 状态：活跃
- 关键词：m1、m2、bff、hono、迁移、测试、闭环
- 相关模块：数据层、后端、鉴权

## 摘要

**T-M1-1 / T-M2-1..5 完成**（BACKLOG #003 代码侧就绪，联调待用户账号操作）：

1. `supabase/migrations/0001_init.sql`：三表 + CHECK + RLS（anon 只读；profiles 仅本人可读）+ updated_at 触发器 + 新用户自动建 profile + site_config 种子行 + Storage 公开 bucket `site-assets`（admin 写入策略）。幂等可重复执行。
2. BFF（`bff/`，Hono 4 + Vitest）：骨架自 CourseCore 移植。移植差异均已注明——`lib/supabase.ts` 新增 `prefer`（分页 count=exact）与 insert/update/remove（写路径）；`auth.ts` 抽出 `authenticateToken` 纯函数并**删除 requireRole**（L-001）；`adminAuth.ts` 用服务端 `profiles.role` 判管理员；`app.ts` 缓存 TTL 换 `GIGS_CACHE_TTL`（默认 60s）、cors 增 PATCH/DELETE、onError 放行 HTTPException。
3. 测试 31 用例全绿（validators 15 + gigs 路由 16）：PT-GIG-01 状态机 3×3 全组合、PT-GIG-02 列表可见性、PT-GIG-03 写门三态+无变更断言、CT-GIG-001 契约形状、TC-VIEW-001/002/006、TC-ADMIN-001..006。覆盖矩阵已同步「已自动化」。
4. `npm run build:bff` 产物 functions/api/[[route]].js（83.3kb）验证通过；根与 bff typecheck 全绿。

## 实施中钉死并回补 spec 的点（v0.2.1）

- subject 筛选值含 `*` / `%` → 422 VALIDATION_ERROR（防 ilike 通配符语义漂移）。
- validateGigPatch 曾把 status 写成 undefined（GigCreate 无该字段）——被 TC-ADMIN-004/005 测试当场抓获并修复；spec 契约本身无误。

## 用户操作清单（T-M1-2 / T-M1-3，Agent 无法代办）

1. 新建独立 Supabase 项目 → SQL Editor 执行 `supabase/migrations/0001_init.sql`。
2. Authentication 关闭邮箱确认（或自行管理）→ 注册管理员账号（邮箱+密码）→ SQL 提权：`update profiles set role='admin' where id='<uuid>';`
3. Storage 上传微信二维码到 `site-assets` bucket → 之后在 /admin/settings 回写 qr_image_url 与 wxid（当前种子为占位值）。
4. Cloudflare：创建 Pages 项目（构建命令 `npm run build`，输出 dist）→ `wrangler kv namespace create RATE_LIMIT_KV` 并在 wrangler.toml 填 id → `wrangler pages secret put SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY`。
5. 本地联调：复制 `.dev.vars.example` 为 `.dev.vars` 填真钥 → `npx wrangler pages dev`（或 `cd bff && npm run dev`）→ 验证 `/api/v1/healthz`。
