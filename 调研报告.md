# CS_tutoring 家教平台 · 项目调研报告

> 日期：2026-08-29 · 性质：纯调研，未改任何代码
> 调研对象：CourseCore（`C:\Users\vitoriga\OneDrive\Desktop\CourseCore`）+ 新平台需求
> 更新（2026-08-29 下午）：文中决策点已全部由用户确认，工作区已按 Agent-Harness-Skills 的 bootstrap 流程初始化；决策记录见 `decisions/001-v1-init-cs-tutoring.md`，当前状态见第 6 节。

## 0. 方法论适用判断（KB 工作区约定）

已通读 D:\KB 方法论必读核心集：`spec-kit/spec-driven.md`、`sample-specship/steering/specship-workflow.md`、`sample-specship/steering/shared/quality-bar.md`。

- 本次任务对应 SpecShip 流程的 RECON（侦察）阶段，只产出调研结论，不进入 PLAN/BUILD。
- 新平台规模判断：单管理员 + 公开单子列表 + 详情页 + 联系微信，属**小型项目 → mini-spec 重量**（spec 一份即可，不必上 spec 三件套 + 质量门脚本）；但 quality-bar 的反 slop 清单（空态/加载态/错误态/375px 响应式/键盘可达）照常作为验收标准。
- 项目层 AGENTS.md 尚未生成。按方法论需先给用户确认草案再落盘，列为下一步，不在本次擅自创建。

## 1. CourseCore 现状（代码与文档核实过的事实基线）

CourseCore 是大学生基础课学习平台（刷题/课程/错题本），与家教平台业务无关，但**基础设施正是"后端不变"要沿用的东西**：

```text
浏览器
  ├─ 前端 SPA（Vite 5 + 原生 JS + Tailwind 3）   ← 这一层整个换掉
  ├─ Cloudflare Pages（dist/ 静态 + 同域 /api/*）
  │    └─ Pages Function = Hono BFF 打包产物 functions/api/[[route]].js
  └─ Supabase（Auth + Postgres + RLS；service_role 只在 BFF 服务端）
```

关键事实：

| 项 | 现状 |
|---|---|
| 前端 | Vite 5 + 原生 JS ESM（无框架）+ Tailwind 3.4，views/services 手写路由 |
| BFF | Hono 4（TypeScript，`bff/src/`），一份代码支持 Worker / Pages Function 两种部署；限流（KV 可选，未绑定自动降级）、边缘缓存、安全头中间件齐全 |
| 登录 | Supabase Auth **邮箱+密码**（`signUp` / `signInWithPassword` / `resetPasswordForEmail`），前端拿 session JWT 带 `Authorization: Bearer` 调 BFF |
| BFF 鉴权 | `middleware/auth.ts`：拿 token 调 `SUPABASE_URL/auth/v1/user` 验证；有 `verifyAuth / optionalAuth / requireRole` |
| 管理员判定 | `profiles` 表（`id, role, display_name, avatar_url`），`role='admin'`；前端 `state.user.role === 'admin'` |
| 部署 | `wrangler pages deploy dist`，密钥走 `wrangler pages secret`；`VITE_*` 构建期注入 |
| 活跃度 | 仓库持续维护中（最近提交 2026-08-12，正在做权限收敛迁移） |

可直接搬走的代码骨架：`bff/src/`（app.ts + 中间件 + 路由组织）、`scripts/build-bff.js`（打包 Pages Function）、`wrangler.toml` 配置模式、`apiClient.js` 的请求封装思路。

### 1.1 发现的一个坑（新平台要修，不能照抄）

CourseCore 的 BFF `requireRole('admin')` 检查的是 **Supabase Auth 返回的 `user.role`**（即 `auth.users.role`，默认值是 `authenticated`），而管理员标记存在 `profiles.role` 里。也就是说 BFF 侧的 requireRole 实际拦不住普通登录用户，管理员鉴权目前只在前端做。新平台的 admin 接口必须在 BFF 内**服务端查询 `profiles.role`** 再放行（RBAC 查一次库，或把 role 写进 Supabase Auth 的 `app_metadata` 由 JWT 带出）。

另外两个历史遗留设计不要带进新平台：`includeAnswer=true` 过渡参数、构建期把答案打进前端 bundle 的 `src/data/*.js` 静态数据模式（家教平台无此需求，天然规避）。

## 2. "后端不变"的确切含义

成立的部分（架构形态原样沿用）：

- Supabase 承担 Auth + Postgres + RLS；
- Hono BFF 跑在 Cloudflare Pages Functions，同域 `/api/v1`；
- Cloudflare Pages 静态发布，`wrangler` 工作流不变。

不成立的部分（必须新增，属于业务扩展而非架构变更）：

- 家教平台自己的业务表（单子等）——CourseCore 现有表（试卷/题目/错题本）与新业务无关；
- 对应的 BFF 路由（gigs CRUD + 管理员鉴权）。

### 决策点 A（已确认：新建独立 Supabase 项目）：同一个 Supabase 项目，还是新建？

| 方案 | 优点 | 缺点 |
|---|---|---|
| 复用 CourseCore 同一 Supabase 项目 | 账号/密钥现成，profiles 表可直接复用，管理员不用重建 | CourseCore 正在做"逐表收窄 anon 权限"的迁移，混入新表会互相牵制；schema 污染；出事故一损俱损 |
| 新建独立 Supabase 项目（推荐） | RLS/表完全独立，不影响 CourseCore 收敛工作；坏了可整库删 | 要重建管理员账号、配一次新密钥（约 10 分钟一次性成本） |

推荐：**新建独立 Supabase 项目，复用的是"架构模式 + 代码骨架"，不共用数据库实例**。"后端不变"理解为技术栈与部署形态不变。

## 3. 新平台需求拆解（家教平台）

### 3.1 角色与核心流程

- 管理员（你）：发布/编辑/下架家教单，维护联系方式。
- 大学生（接单方）：浏览单子列表 → 看详情 → 通过微信联系管理员 → 接单。

核心闭环：管理员发布 → 学生浏览/筛选 → 详情页联系微信 → 管理员把单子标记"已接/关闭"。

### 3.2 单子（gig）字段草案

> 2026-08-29 修订：本节为调研期草案，**字段以 `specs/spec.md` v0.2.0 为准**（region 无条件必填；新增 student_gender / student_info；requirements 明确为「对老师的要求」）。

```text
gigs:
  id            uuid pk
  title         text          如"高二数学 一对一"
  subject       text          数学/物理/英语/编程…
  grade_level   text          小学/初中/高中/大学
  mode          text          线上 / 线下
  region        text null     线下时的区域
  rate          text          时薪或面议，"150/小时"
  schedule      text          可授课时间（每周六全天…）
  requirements  text          学员情况与要求（长文本，Markdown）
  status        text          open | matched | closed
  contact_wxid  text null     该单专用微信（留空则用全局配置）
  published_by  uuid          → auth.users.id
  created_at / updated_at timestamptz
```

全局联系方式建议单独一张小表（`site_config: wxid, qr_image_url, notice`），后台可改，不用重新发版。

### 3.3 微信连接的现实约束（重要）

个人微信**没有公开 API** 让 H5 网页直接跳转发起好友申请，`weixin://` 私有 scheme 现在基本被封。可行方案对比：

| 方案 | 体验 | 门槛 |
|---|---|---|
| 二维码 + 复制微信号（推荐 v1） | 详情页点"联系管理员接单"→ 弹层展示管理员微信二维码 + 一键复制微信号；用户在微信内长按即可识别二维码 | 零门槛，稳定可靠 |
| 企业微信"联系我"链接/客服 | 点击直接拉起微信聊天，体验最好 | 要注册企业微信并配置（免费但多一步运营动作） |
| 公众号客服/小程序 | — | 需认证主体，重 |

v1 建议二维码 + 复制 wxid，后续可平滑加企业微信链接字段。

### 3.4 移动端适配要点

目标用户的打开路径大概率是**微信/QQ 群里分享链接 → 微信内置浏览器打开**，适配以微信 H5 为第一优先：

- `viewport-fit=cover` + `env(safe-area-inset-*)` 处理 iOS 底部横条；
- 布局用 `100dvh` 不用 `100vh`（微信浏览器地址栏收缩）；
- 底部 Tab 导航（App 感）+ 单子卡片流，操作按钮贴底部拇指区；
- 弹层里的二维码在微信内置浏览器里可长按识别（原生支持，无需额外代码）；
- 分享卡片自定义（JS-SDK）需要认证公众号 + JS 安全域名，**v1 不做**，用默认分享即可，列为后续项。

### 3.5 学生要不要登录（已确认：v1 免登录，仅管理员登录）

| 方案 | 说明 |
|---|---|
| v1 学生免登录（推荐） | 浏览、看联系方式零门槛，转化最高；只做管理员登录。RLS 只需 anon SELECT + admin 写 |
| 学生注册登录 | 能做"我的报名/单子收藏/浏览历史"，需要 profiles 扩展、邮箱验证运营成本 |

v1 推荐免登录，把"申请记录"留到 v2（加一张 `applications` 表即可，不影响现有结构）。

## 4. React 前端技术选型建议

结合三点约束：换 React、"好看一点"、审美基准为**终末地-莱因科技-明日方舟「工业科研终端感」**（近黑/冷灰底、安全黄警示色、少量冰蓝/洋红能量光；冷静精密克制，避免赛博霓虹堆叠）——已确认不用 CourseCore 的黑白墨绿。审美规范与可移植组件库在 `D:\KB\项目案例\01-软件系统\前端组件\终末地-莱因科技-明日方舟风格\`：`extracted-components/`（按 layout/cards/form/dialog/navigation/decoration/theme 分类，theme/ 集中放设计令牌）+ 同目录 AGENTS.md（风格定义）。React 化移植时遵守其提取铁律：动画全量搬运逐字一致、令牌入 theme、组件带来源注释头。

| 层 | 选型 | 理由 |
|---|---|---|
| 构建 | Vite + React 19 + TypeScript | 与 BFF 的 TS 统一；CourseCore 的 Vite 工作流经验直接复用 |
| 路由 | react-router v7 | 标准选择 |
| 数据 | TanStack Query v5 | 列表缓存/刷新/加载态，替代手写 fetch 状态机 |
| 样式 | Tailwind CSS（新项目可直接上 v4）+ 自绘组件 | 沿用已有 Tailwind 经验；自绘组件才能做出非模板化的"好看"，符合反 slop 偏好；必要时引入无样式的 headless 组件（Radix primitives）补交互 |
| 微动效 | framer-motion（可选） | 弹层/列表过渡，克制使用 |
| 图标 | lucide-react | 单一图标族 |
| 状态 | Zustand（仅在需要时） | 全局状态很少（auth session），可能根本不需要 |

不建议用 Ant Design Mobile 之类成品库做主皮肤：出来的观感是"标准后台模板脸"，与你"好看一点、反 AI slop"的目标冲突；它可以作为应急组件来源，不作为设计系统。

### 目录结构草案（对齐 CourseCore 的职责划分习惯）

```text
CS_tutoring/
├── src/                  # React 前端（页面、组件、services）
├── bff/src/              # Hono BFF（从 CourseCore bff/src 搬骨架后改造）
├── functions/api/[[route]].js   # 打包产物，不手工编辑
├── scripts/build-bff.js
├── wrangler.toml
└── docs/                 # spec、调研、决策记录
```

### BFF 路由草案

```text
GET    /api/v1/gigs?status=&subject=&grade_level=&mode=   # 公开，只返回 open 列表默认
GET    /api/v1/gigs/:id                                   # 公开
GET    /api/v1/site-config                                # 公开（联系方式弹层用）
POST   /api/v1/gigs            # 管理员（服务端查 profiles.role）
PATCH  /api/v1/gigs/:id        # 管理员
DELETE /api/v1/gigs/:id        # 管理员
PATCH  /api/v1/site-config     # 管理员
GET    /api/v1/healthz
```

沿用 CourseCore 的限流/安全头/缓存中间件；gigs 列表缓存 TTL 设短（如 60s），管理员改单后可主动失效或接受短暂延迟。

### 页面清单

- 首页/单子列表：筛选 chips（科目/年级/线上线下）、卡片流、骨架屏、空态（"暂时没有新单子"）、下拉刷新；
- 单子详情：完整要求展示 + 底部固定"联系管理员接单"按钮 → 弹层（二维码 + 复制微信号 + 提示文案）；
- 管理端 `/admin`：登录、发布/编辑表单、单子列表改状态、联系方式配置；移动端可用的全屏后台（延续 CourseCore 后台习惯）；
- 简单的"关于/规则"页（可选）。

## 5. 风险清单

1. **管理员鉴权坑**（见 1.1）：必须在服务端查 role，不能沿用 requireRole 现状。
2. **微信 H5 的不可控性**：微信内置浏览器对某些 CSS/弹层行为有怪癖，真机（iOS + Android 微信）回归不能省。
3. **二维码图片托管**：放 Supabase Storage（公开 bucket）即可，不要 base64 塞接口响应。
4. **CourseCore 不动**：新平台代码放独立仓库/目录（本目录），不污染 CourseCore 仓库；两边只共享"骨架模式"，不共享运行时。
5. 免登录方案下，anon 可无限拉取列表——BFF 限流中间件已有，保留即可。

## 6. 当前状态与下一步（2026-08-29 更新）

已完成：

1. 决策全部确认（见 `decisions/001-v1-init-cs-tutoring.md`）：新建独立 Supabase 项目、v1 学生免登录、单子字段按草案、审美换终末地-莱因科技-明日方舟风格；
2. 工作区初始化完成（bootstrap-agent-workspace 敏捷迭代套餐）：`AGENTS.md` + `decisions/` + `BACKLOG.md` + `tools/path_align_hooks/`（三项钩子试跳通过），ZCode Stop 钩子接线至 `.zcode/config.json`。

下一步（对应 BACKLOG 编号）：

1. #001 用 spec-writing 写根级 Spec 三件套（含反 slop 验收标准），先确认再动工；
2. #002 前端脚手架（Vite + React + TS + Tailwind）+ git init，回填真实验证命令；
3. #003 新建 Supabase 项目建表 + 搬 BFF 骨架改造为 gigs 路由；
4. #004/#005 移动端适配基线与联系管理员弹层。
